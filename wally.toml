[package]
name = "ddust1n/replica"
description = "Fork of Replica - Roblox server to client state replication solution which lets the developer subscribe certain players to certain states"
version = "0.0.1"
license = "MIT"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/**", "default.project.json", "LICENSE", "wally.toml"]

[dependencies]